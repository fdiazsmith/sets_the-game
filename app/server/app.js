/**
  *
  * S E R V E R
  *
  *
  */

if (Meteor.isServer) {
  // Meteor.startup(function () {
  //   // code to run on server at startup
  // });
  //process.env accesses the variable in the env in shell
  // var Example_shell_var = process.env.CUSTOM_VAR || "undefined probably dev";
  // console.log(Example_shell_var);

  //Meteor settings
  // console.log(Meteor.settings);
}
